/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['marathon-web-assets.s3.ap-south-1.amazonaws.com'], // Add the hostname here
    },
  };
export default nextConfig;
